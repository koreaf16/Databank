{
  "name": "databank-mockup-system",
  "private": true,
  "version": "0.1.0",
  "description": "DATABANK IT-SCO mockup system split into frontend and backend Node.js workspaces.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node kill-dev-ports.js && concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
